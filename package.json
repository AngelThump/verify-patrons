{
  "name": "verify-patrons",
  "version": "1.0.0",
  "description": "verify if patrons are actually patrons, if not patch them.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.0"
  }
}
